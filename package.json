{
  "name": "mac-insurance",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ mac-insurance",
    "create-local": "graph create --node http://localhost:8020/ mac-insurance",
    "remove-local": "graph remove --node http://localhost:8020/ mac-insurance",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mac-insurance"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
